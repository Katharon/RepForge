# Slice Status

Status values:

- `planned`
- `in_progress`
- `ready-to-commit`
- `done`
- `blocked`
- `deferred`

All slices are planned until implemented and committed.

## Current status

- Slice 00: done repository governance and docs baseline.
- Slice 01: done Flutter project bootstrap.
- Slice 02: done analysis options, formatting, and test gates.
- Slice 03: done architecture skeleton and composition root.
- Slice 04: done design tokens and app theme.
- Slice 05: done navigation shell and route map.
- Slice 06: done training-log domain foundation.
- Slice 07: done analytics formula domain tests.
- Slice 08: done Drift local database foundation.
- Slice 09: done training-log repository implementation and mappers.
- Slice 10: done dependency injection wiring.
- Slice 11: done official exercise catalog foundation.
- Slice 12: done workout group assignment foundation.
- Slice 13: done exercise set timeline paging.
- Slice 14: done add/edit/delete workout set foundation.
- Slice 15: done set labels and comments foundation.
- Slice 16: done rest timer domain and state foundation.
- Slice 17: done local notifications for rest timer.
- Slice 18: done exercise analytics use cases.
- Slice 19: done analytics charts and range selector UI.
- Slice 20: done estimated 1RM feature.
- Slice 21: done Today dashboard.
- Slice 22: done Settings and user profile foundation.
- Slice 23: done onboarding, initial groups, and bundled sample data.
- Slice 24: done import/export local backup.
- Slice 25: done database migrations and data integrity hardening.
- Slice 26: done search, filter, sort, and archive flows.
- Slice 27: done accessibility and responsive layout pass.
- Slice 28: done golden tests and visual regression baseline.
- Slice 29: done integration/E2E workout logging flow.
- Slice 30: done security and privacy hardening.
- Slice 31: done performance and large-history optimization.
- Slice 32: done entitlement domain and premium gates.
- Slice 33: done app-store purchase gateway integration.
- Slice 34: done purchase verification boundary and cache policy.
- Slice 35: done optional authentication boundary.
- Slice 36: done Firebase optional integration boundary.
- Slice 37: done optional sync metadata boundary.
- Slice 38: done remote push notification boundary.
- Slice 39: done CI/CD release pipeline.
- Slice 40: done app icon, splash, and store metadata.
- Slice 41: done beta release candidate hardening.
- Slice 42: done production release checklist.
- Slice 43: done official exercise catalog expansion and manifest.
- Slice 44: done user profile, focus, and equipment model expansion.
- Slice 45: done muscle activation model.
- Slice 46: done muscle balance detection.
- Slice 47–54: planned recovery, recommendations, quick session, wearable design, and social design.
- Slice 55–56: planned legal/compliance/resilience and backward-compatibility hardening.

## Critical project decision

The official exercise catalog must not depend on a paid cloud database. Catalog updates are shipped as versioned app assets through app releases/patches and imported locally.

## Update rule

After every implemented slice, Codex must update this file with:

- date,
- slice number,
- status,
- commit hash,
- short implementation summary,
- validation commands run,
- follow-ups.

## v5 documentation status

Planning docs were updated with:

- multilingual/system-locale decision,
- MVP boundary: tracker + groups + analytics,
- small official catalog scope,
- equipment inventory and max-load modeling,
- Setgraph-inspired but original UI direction,
- freemium monetization strategy,
- training disclaimer/safety boundary,
- naming candidate list.

Slice 00 has completed documentation/governance implementation and is committed. Slice 01 has completed Flutter bootstrap implementation and is committed. Slice 02 has completed quality-gate implementation and is committed. Slice 03 has completed architecture skeleton implementation and is committed. Slice 04 has completed design-system implementation and is committed. Slice 05 has completed navigation-shell implementation and is committed. Slice 06 has completed training-log domain foundation implementation and is committed. Slice 07 has completed analytics formula domain implementation and is committed. Slice 08 has completed Drift local database foundation implementation and is committed. Slice 09 has completed training-log repository implementation and is committed. Slice 10 has completed dependency-injection wiring and is committed. Slice 11 has completed the official exercise catalog foundation and is committed. Slice 12 has completed the workout group assignment foundation and is committed. Slice 13 has completed exercise set timeline paging and is committed. Slice 14 has completed the add/edit/delete workout set foundation and is committed. Slice 15 has completed the set labels and comments foundation and is committed. Slice 16 has completed the rest timer domain and state foundation and is committed. Slice 17 has completed local rest timer notification scheduling and is committed. Slice 18 has completed exercise analytics use cases and is committed. Slice 19 has completed analytics charts and range selector UI and is committed. Slice 20 has completed the estimated 1RM feature and is committed. Slice 21 has completed the Today dashboard and is committed. Slice 22 has completed the settings and user profile foundation and is committed. Slice 23 has completed onboarding, initial groups, and bundled sample data and is committed. Slice 24 has completed the import/export local backup foundation and is committed. Slice 25 has completed database migration and data integrity hardening and is committed. Slice 26 has completed local search, filter, sort, and archive flows and is committed. Slice 27 has completed accessibility and responsive layout hardening and is committed. Slice 28 has completed the golden-test baseline and is committed. Slice 29 has completed the integration-style workout logging flow harness and is committed. Slice 30 has completed security and privacy hardening and is committed. Slice 31 has completed performance and large-history optimization and is committed. Slice 32 has completed the entitlement domain and premium gates and is committed. Slice 33 has completed app-store purchase integration. Slice 34 has completed the purchase verification boundary and entitlement-cache policy. Slice 35 has completed the optional authentication boundary. Slice 36 has completed the Firebase optional integration boundary. Slice 37 has completed the optional sync metadata boundary. Slice 38 has completed the remote push notification boundary. Slice 39 has completed the CI/CD release pipeline foundation. Slice 40 has completed app branding and store metadata documentation. Slice 41 has completed beta release-candidate hardening. Slice 42 has completed the production release checklist. Slice 43 has completed the official exercise catalog expansion and manifest. Slice 44 has completed the user profile, focus, and equipment model expansion. Slice 45 has completed the muscle activation model foundation. Slice 46 has completed muscle balance detection.

## Slice log

| Date | Slice | Status | Commit | Summary | Validation | Follow-ups |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-26 | 00 — Repository governance and docs baseline | done | this commit | Normalized root governance files, GitHub templates, and Codex slice instructions for the RepForge v9 baseline. | `git status --short`; `find . -maxdepth 3 -type f \| sort`; required `test -f` checks; RepForge/guardrail `rg` checks. `markdownlint` skipped because it is not installed. | None. |
| 2026-05-27 | 01 — Flutter project bootstrap | done | this commit | Bootstrapped Android/iOS Flutter app at repository root with package `repforge`, localized RepForge placeholder shell, English/German ARB files, and widget smoke tests. | `git status --short`; `flutter --version`; `flutter doctor -v`; `flutter pub get`; `flutter gen-l10n`; `dart format --set-exit-if-changed lib test`; `flutter analyze`; `flutter test`; `find . -maxdepth 3 -type f \| sort`; requested `rg` checks. | None. |
| 2026-05-27 | 02 — Analysis options, formatting, test gates | done | this commit | Tightened practical Flutter analyzer settings, documented validation commands, added `scripts/check.sh`, and added a minimal GitHub Actions quality workflow. | `git status --short`; `flutter --version`; `flutter pub get`; `flutter gen-l10n`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; `find . -maxdepth 3 -type f \| sort`; requested `rg` checks. | None. |
| 2026-05-27 | 03 — Architecture skeleton and composition root | done | aa1870e | Moved the localized placeholder app into `lib/src/app`, added a minimal explicit composition root, documented `core`/`features` extension points, and updated widget smoke tests. | `git status --short`; `flutter pub get`; `flutter gen-l10n`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `find lib test -maxdepth 5 -type f \| sort`; requested `rg` guardrail checks; `scripts/check.sh`. Flutter/Dart commands required SDK-cache escalation after sandbox read-only failures. | None. |
| 2026-05-27 | 04 — Design tokens and app theme | done | 0e72d36 | Added dark-first Material 3 theme tokens, metric colors, numeric typography helpers, the feature-neutral `AppCard`, and applied the RepForge theme to the localized placeholder app. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `find lib test -maxdepth 6 -type f \| sort`; requested `rg` guardrail checks; `scripts/check.sh`. | None. |
| 2026-05-27 | 05 — Navigation shell and route map | done | 9b9d23e | Added a minimal `go_router` route map, localized mobile navigation shell, placeholder destinations for Today, Groups, Exercises, Analytics, and Settings, and widget tests for routes/locales/taps. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `find lib test -maxdepth 6 -type f \| sort`; requested `rg` guardrail checks; `scripts/check.sh`. | None. |
| 2026-05-27 | 06 — Domain foundation for training log | done | 16f7501 | Added pure-Dart training-log domain value objects, stable IDs, exercise references with snapshots, the `WorkoutSet` entity, and the domain-only `WorkoutSetRepository` contract. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `find lib test -maxdepth 8 -type f \| sort`; requested training-log and dependency guardrail `rg` checks; `scripts/check.sh`. | None. |
| 2026-05-27 | 07 — Analytics formula domain tests | done | 7bd2809 | Added pure-Dart analytics formula identity, Epley estimated 1RM, workout-set summary aggregation, and period comparison helpers with focused domain tests. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `find lib test -maxdepth 8 -type f \| sort`; requested analytics/training-log and no-cloud guardrail `rg` checks; `scripts/check.sh`. | None. |
| 2026-05-27 | 08 — Drift local database foundation | done | da83acc | Added Drift/SQLite dependencies, schema version 1, generated Drift database code, and the historical `workout_sets` table with stable exercise reference snapshots and in-memory schema tests. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `find lib test -maxdepth 9 -type f \| sort`; requested Drift/domain/no-cloud guardrail `rg` checks; `scripts/check.sh`. | Harden `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks in a later analytics/domain mini-slice. |
| 2026-05-27 | 09 — Repository implementations and mappers | done | 19fb9db | Added `WorkoutSetMapper` and `DriftWorkoutSetRepository` for stable-ID upserts, deterministic exercise/session queries, snapshot-preserving row mapping, and invalid custom catalog snapshot detection. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `find lib test -maxdepth 9 -type f \| sort`; requested repository/mapper, domain purity, UI leakage, and no-cloud guardrail `rg` checks; `scripts/check.sh`. | Harden `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks in a later analytics/domain mini-slice. |
| 2026-05-27 | 10 — Dependency injection wiring | done | 2f25df4 | Wired the explicit composition root to create the local Drift database through a factory, expose the `WorkoutSetRepository` contract backed by `DriftWorkoutSetRepository`, and close owned resources idempotently from `AppDependencies`/`RepForgeApp`. | `flutter pub get` passed after SDK-cache escalation; `HOME=/tmp PUB_CACHE=/home/luki/.pub-cache /home/luki/flutter/bin/cache/dart-sdk/bin/dart run build_runner build --delete-conflicting-outputs`; `HOME=/tmp /home/luki/flutter/bin/cache/dart-sdk/bin/dart format --output=none --set-exit-if-changed .`; `HOME=/tmp /home/luki/flutter/bin/cache/dart-sdk/bin/dart analyze`; `git diff --check`; requested `find`/`rg` guardrails. | Keep hardening `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks for a later analytics/domain mini-slice. |
| 2026-05-27 | 11 — Exercise catalog feature | done | acf802a | Added the versioned bundled official exercise catalog asset, pure-Dart catalog models/query contract, parser validation, Drift schema v2 catalog tables, idempotent official import, and pagination-ready Drift query repository without composition wiring or app-start import behavior. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; requested `find`/`rg` guardrails; `scripts/check.sh`. | Wire catalog dependencies and app-start import in a later slice when the app needs the catalog at runtime. Keep hardening `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks for a later analytics/domain mini-slice. |
| 2026-05-27 | 12 — Workout groups and exercise assignment foundation | done | 436deb3 | Added pure-Dart workout group and assignment domain contracts, Drift schema v3 group tables, mapper/repository persistence, deterministic paginated group and assignment queries, nullable UTC archive timestamp round-trip, and invalid custom assignment catalog-snapshot validation. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; requested `find`/`rg` guardrails; `scripts/check.sh`. | Wire workout group repository into composition in a later UI/application slice. Keep hardening `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks for a later analytics/domain mini-slice. |
| 2026-05-27 | 13 — Exercise detail set timeline | done | 482e6c8 | Added pure-Dart workout-set timeline query/cursor/page types and Drift keyset paging for one exercise over stable source/id, `performedAt`, and stable workout-set ID without schema or composition changes. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; requested timeline/domain/no-cloud guardrails; `scripts/check.sh`. | Build UI/BLoC exercise detail flows in a later slice. Keep hardening `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks for a later analytics/domain mini-slice. |
| 2026-05-27 | 14 — Add/edit workout set form | done | 7e420a7 | Added pure-Dart training-log application use cases for saving, updating, and deleting workout sets, a compact `WorkoutSetForm` input parser, and Drift targeted delete support while preserving existing IDs, exercise snapshots, and session links on edit. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; focused `flutter test` for training-log application/repository/domain tests; direct `dart analyze`; `git diff --check`; requested `rg` guardrails. Full `flutter analyze`, full `flutter test`, and `scripts/check.sh` were blocked after escalation became unavailable by Flutter SDK cache writes in the sandbox. | Keep hardening `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks for a later analytics/domain mini-slice. |
| 2026-05-27 | 15 — Set labels and comments | done | 2417545 | Added pure-Dart `WorkoutSetLabel`, threaded labels through `WorkoutSet`, `WorkoutSetForm`, mapper, and Drift repository behavior, bumped Drift to schema v4 with additive `workout_sets.set_label`, and preserved existing comment behavior. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested label/domain-purity/no-cloud guardrail `rg` checks. | Labels may later influence analytics, coach guidance, stimulus, and muscle-load calculations. Keep hardening `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks for a later analytics/domain mini-slice. |
| 2026-05-27 | 16 — Rest timer domain and state | done | dbab6e3 | Added pure-Dart rest timer duration/status/snapshot types, injectable time provider, deterministic in-memory controller, and countdown presentation state without persistence, real timer loops, platform notifications, or Firebase. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested rest-timer/no-notification/domain-purity guardrail `rg` checks. | Slice 17 owns local notification scheduling. Keep hardening `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks for a later analytics/domain mini-slice. |
| 2026-05-27 | 17 — Local notifications for rest timer | done | 2d7354a | Added a fakeable rest-timer notification gateway and coordinator, wired it through composition, and added a `flutter_local_notifications` Android/iOS adapter with local-only schedule/cancel behavior for volatile rest timers. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested notification/no-FCM/domain-purity guardrail `rg` checks. | Add full notification settings/UX only in a later explicit slice. Keep hardening `FormulaIdentity` and `EstimatedOneRepMax` validation outside assert-only checks for a later analytics/domain mini-slice. |
| 2026-05-28 | 18 — Exercise analytics use cases | done | 90ad96a | Added pure-Dart exercise analytics read models and a bounded WorkoutSet timeline use case for per-exercise set count, repetitions, volume, average kg/rep, estimated 1RM, previous comparable-session deltas, and equal-length time-window deltas. Hardened analytics formula identity and estimated 1RM validation deterministically. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested analytics/no-remote/domain-purity guardrail `rg` checks. | Slice 19 owns charts and range-selector UI. |
| 2026-05-28 | 19 — Analytics charts and range selector UI | done | 9b3c5ca | Added a localized Analytics tab UI backed by a fakeable Slice 18 use-case loader, metric and range selectors, loading/empty/error/success states, compact summary cards, and simple two-bar chart cards without new chart packages or remote analytics. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested analytics/no-remote/domain-purity guardrail `rg` checks. `build_runner` passed while warning that `--delete-conflicting-outputs` is ignored by the installed version. | Connect the analytics surface to exercise selection/detail flows in later UI slices. |
| 2026-05-28 | 20 — Estimated 1RM feature | done | e8cc7b3 | Added estimated 1RM formula identity to the exercise analytics read model and rendered a focused Analytics 1RM card with current estimate, previous-window value, Epley v1 label, unavailable state, zero-load behavior, and existing selector/chart integration. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested 1RM/no-remote/domain-purity guardrail `rg` checks. `build_runner` passed while warning that `--delete-conflicting-outputs` is ignored by the installed version. | Later exercise-detail flows should pass the selected exercise into Analytics instead of the current default exercise. |
| 2026-05-28 | 21 — Today dashboard | done | d412951 | Replaced the Today placeholder with a localized local-first dashboard, fakeable loader seam, daily set/volume cards, last logged set summary, rest timer card, quick-action placeholder, analytics hint, and loading/empty/error/success states. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested Today/no-cloud/domain-purity guardrail `rg` checks. `build_runner` passed while warning that `--delete-conflicting-outputs` is ignored by the installed version. | Add a focused all-sets-for-day local query and wire real daily set/volume/last-set data into `TodayDashboardLoader` in a later logging/dashboard slice. |
| 2026-05-28 | 22 — Settings and user profile foundation | done | 37a189b | Added pure-Dart settings/profile domain values, load/save/reset use cases, Drift schema v5 local persistence, repository mappers, composition-root wiring, localized Settings UI controls, and focused domain/repository/use-case/widget coverage. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested settings/no-cloud/domain-purity guardrail `rg` checks. | Apply stored language/theme preferences to the app shell in a later app-shell slice if desired; Slice 23 owns onboarding wizard and starter data. |
| 2026-05-28 | 23 — Onboarding, initial groups, and bundled sample data | done | b1c49ee | Added skippable local onboarding, onboarding status persistence, settings-save completion, bundled starter-group JSON templates, optional starter workout group creation, and focused domain/use-case/data/widget coverage. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested onboarding/no-remote/domain-purity guardrail `rg` checks. | Add persisted home-gym max-load fields in a later settings/profile model slice before recommendation logic consumes them. |
| 2026-05-28 | 24 — Import/export local backup | done | ee5ea22 | Added versioned JSON backup DTOs, deterministic validation, Drift-backed export/import services, composition-root use cases, additive/upsert import policy, and focused domain/use-case/data tests without platform file IO or cloud services. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested backup/no-remote/domain-purity guardrail `rg` checks. | Add user-facing file picker/share UI and optional CSV export in later explicit slices. |
| 2026-05-28 | 25 — Database migrations and data integrity hardening | done | bbd16e4 | Added current-schema validation, a non-empty schema v1 migration fixture foundation, broader constraint tests, deterministic persistence integrity findings, report-only safe repair with explicit blank-label normalization, and archive/delete/import policy tests. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested migration/integrity/no-cloud/no-domain-leakage guardrail `rg` checks. | Add generated historical Drift schema snapshots for future schema bumps; current coverage uses an honest v1-style fixture because prior snapshots did not exist. |
| 2026-05-28 | 26 — Search, filter, sort, and archive flows | done | 961beb7 | Added bounded training-log history search/filter/sort pagination and workout-group search/sort/archive listing behavior while preserving workout sets, assignments, and official catalog rows. | `git status --short`; `git rev-parse --short HEAD`; `HOME=/tmp /home/luki/flutter/bin/cache/dart-sdk/bin/dart format --output=none --set-exit-if-changed .`; `HOME=/tmp /home/luki/flutter/bin/cache/dart-sdk/bin/dart analyze`; `git diff --check`; requested guardrail `rg` checks. Flutter tests and full Flutter validation were blocked after SDK-cache escalation became unavailable in the sandbox. | None. |
| 2026-06-01 | 27 — Accessibility and responsive layout pass | done | 14c3d5f | Added semantic labels and summaries for app navigation, Today metrics/rest timer, Analytics metrics/charts, Settings controls, and Onboarding controls; hardened larger text-scale layouts; added 48px minimum button targets and a constrained responsive sliver-list helper for current screens. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `scripts/check.sh`; requested accessibility, golden, no-cloud, and domain-purity `rg` guardrails. `build_runner` passed while warning that `--delete-conflicting-outputs` is ignored by the installed version. | Slice 28 owns golden tests and visual regression baselines. |
| 2026-06-01 | 28 — Golden tests and visual regression baseline | done | 99bb230 | Added a deterministic Flutter golden-test harness and small baseline for `AppCard`, Today success, Analytics success, Settings defaults, and Onboarding welcome snapshots. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test --update-goldens`; `flutter test`; `scripts/check.sh`; requested golden, no-cloud, and domain-purity `rg` guardrails; `git diff --check`; stale Slice 28 path `rg` check. `build_runner` passed while warning that `--delete-conflicting-outputs` is ignored by the installed version. | Keep the golden suite small; Slice 29 owns integration/E2E workout logging flow. |
| 2026-06-01 | 29 — Integration/E2E workout logging flow | done | 6c9d23c | Added a deterministic integration-style widget harness for official exercise search/selection, set logging, edit persistence, history read-back, Today dashboard reaction, Analytics reaction, and fake rest-timer scheduling using in-memory Drift and no device services. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test integration_test`; `flutter test test/src/integration`; `scripts/check.sh`; requested integration/logging/no-cloud/domain-purity guardrails. | Wire the production Today loader to real daily set aggregation in a later dashboard/logging slice. |
| 2026-06-01 | 30 — Security and privacy hardening | done | 02a105c | Added backup export privacy warning metadata, log-safe backup validation exception strings, backup JSON redaction for diagnostics, and privacy-safe rest-timer notification scheduling that replaces workout-specific content before it reaches the platform gateway. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/integration`; `scripts/check.sh`; requested privacy/no-cloud/domain-purity/logging guardrails. | Consider optional encrypted database/export support only in a later explicit slice; localize any future user-facing backup warning UI when export UI is added. |
| 2026-06-01 | 31 — Performance and large-history optimization | done | 35f4ed3 | Added additive Drift indexes for workout-set timeline/history/session reads, bounded Today daily aggregation, analytics scan caps, and deterministic large-seed query coverage. | `git log --oneline -8`; `HOME=/tmp PUB_CACHE=/home/luki/.pub-cache /home/luki/flutter/bin/cache/dart-sdk/bin/dart run build_runner build --delete-conflicting-outputs`; `HOME=/tmp PUB_CACHE=/home/luki/.pub-cache /home/luki/flutter/bin/cache/dart-sdk/bin/dart format .`; `HOME=/tmp PUB_CACHE=/home/luki/.pub-cache /home/luki/flutter/bin/cache/dart-sdk/bin/dart analyze`; focused `dart test` blocked by `sqlite3` native code-asset download with network unavailable; `flutter test` blocked by Flutter SDK cache writes in the sandbox. | Run full Flutter validation once SDK-cache writes and the sqlite3 native asset are available. |
| 2026-06-01 | 32 — Entitlement domain and premium gates | done | 76e0b20 | Added pure-Dart entitlement snapshots, sources, states, Premium feature gates, deterministic gate decisions, a default local/free entitlement source, composition-root wiring, and focused decision tests without store, backend, account, paywall, Drift, or cloud integration. | `git log --oneline -8`; `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/integration`; `scripts/check.sh`; requested entitlement/payment/cloud/network/domain-purity `rg` guardrails. | Slice 33 owns app-store purchase integration; Slice 34 owns receipt/server verification strategy. |
| 2026-06-01 | 33 — App-store purchase integration | done | f8c4b99 | Added pure-Dart purchase products/events/errors/gateway ports, purchase use cases, provisional entitlement mapping, a deterministic fake gateway test seam, official `in_app_purchase` data adapter, and composition-root wiring without paywall UI, backend, account, RevenueCat, Firebase, ads, sync, or trusted verification. | `git log --oneline -8`; focused `flutter test test/src/features/purchases test/src/features/entitlements`; full Slice 33 validation commands. | Slice 34 owns receipt/server verification and trusted entitlement validation. |
| 2026-06-01 | 34 — Purchase verification strategy | done | 3ff3c95 | Added pure-Dart purchase verification request/result/status/source models, a fakeable verification source port, conservative verification-to-entitlement mapping, default unavailable verification wiring, and bounded verified entitlement-cache policy without backend, accounts, raw receipt handling, RevenueCat, Firebase, Supabase, ads, sync, or paywall UI. | `git log --oneline -8`; focused `flutter test test/src/features/purchases test/src/features/entitlements`; full Slice 34 validation commands. | A later provider/backend slice must supply real trusted receipt/server verification before production subscriptions can rely on store purchases. |
| 2026-06-01 | 35 — Authentication abstraction | done | 3bb3a54 | Added pure-Dart auth user/session/identity/provider/failure/status models, fakeable `AuthGateway`, local-only default gateway, status/sign-out use cases, auth session policy, and composition-root wiring without login UI, provider SDKs, backend, sync, account requirement, token persistence, or auth-based Premium unlock. | `git log --oneline -8`; focused `flutter test test/src/features/auth test/src/app/composition_root_test.dart test/widget_test.dart`; full Slice 35 validation commands. | Future auth/provider slices must keep login optional, isolate token handling outside domain, and update privacy docs before adding real providers, accounts, sync, or restore. |
| 2026-06-02 | 36 — Firebase optional integration boundary | done | 4989afa | Added pure-Dart Firebase capability/configuration/status/result models, a fakeable initialization gateway, default disabled/unavailable composition-root wiring, and tests proving Firebase failure or unavailability does not block local MVP dependencies, local auth defaults, or local rest-timer notifications. | `git log --oneline -8`; `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/integration`; `scripts/check.sh`; requested Firebase/cloud/network/domain-purity `rg` guardrails. `build_runner` passed while warning that `--delete-conflicting-outputs` is ignored by the installed version. | Future provider slices must keep Firebase explicit, disabled until configured, privacy-reviewed, and isolated from local tracking, local notifications, auth defaults, catalog import, backups, purchases, entitlements, and sync. |
| 2026-06-02 | 37 — Optional sync design spike and metadata | done | dbf516a | Added pure-Dart sync metadata and conflict-policy types for future user-data sync vocabulary, including local-only defaults, entity-type policy, local versions, optional remote ids, tombstones, and deterministic conflict decisions without schema changes, accounts, Firebase, Firestore, transport, background jobs, upload/download behavior, or production sync. | `git log --oneline -8`; `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/integration`; `scripts/check.sh`; requested sync/cloud/network/domain-purity `rg` guardrails. | Future sync implementation must remain optional, add only additive persistence if needed, keep official catalog patches separate, and update privacy/security docs before any remote provider, account requirement for sync, or data transfer is introduced. |
| 2026-06-02 | 38 — Remote push notification boundary | done | 6901715 | Added pure-Dart Remote Push token, capability, message-type, registration-state, failure, configuration, and gateway vocabulary with default disabled/unavailable composition wiring. Tests prove disabled Remote Push does not request tokens, require auth, activate sync/Firebase, use Remote Push for rest timers, or block local MVP behavior. | `git log --oneline -8`; `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/integration`; `scripts/check.sh`; requested Remote Push/FCM/backend/domain-purity `rg` guardrails. | Future Remote Push provider work must remain opt-in/explicit, keep provider token handling outside domain, avoid rest-timer replacement, and update privacy docs before FCM/APNS token requests, backend registration, or remote message handling. |
| 2026-06-02 | 39 — CI/CD release pipeline | done | 31c2c72 | Upgraded GitHub Actions to `RepForge CI` with PR/push/manual triggers, generated-code freshness checks, formatting, analysis, full tests, integration-style tests, local `scripts/check.sh` parity, and a dependent secret-free Android debug APK artifact upload. | `git log --oneline -8`; `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/integration`; `scripts/check.sh`; local YAML parse check; `flutter build apk --debug`; requested CI/secrets/cloud guardrail `rg` checks. | Future release slices must add signed App Bundle/iOS/TestFlight/store publishing only with explicit secrets, protected environments, privacy/store metadata, and signing ownership decisions. |
| 2026-06-02 | 40 — App icon, splash, and store metadata | done | fb20636 | Validated the existing RepForge launcher icon setup, kept Android/iOS display names aligned, branded native launch backgrounds with the RepForge near-black color, and added English/German store listing and privacy-copy drafts without publishing, signing, cloud, ads, Firebase, backend, or new runtime services. | `git status --short`; `git rev-parse --short HEAD`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/integration`; `scripts/check.sh`; `flutter build apk --debug`; requested branding/store/privacy/secrets/cloud guardrail `rg` checks. | Verify trademark and store-name availability for `RepForge`, add final screenshots/support links/privacy declarations before public release, and add `flutter_native_splash` only if a later slice requires generated branded splash assets. |
| 2026-06-02 | 41 — Beta release candidate hardening | done | 4550651 | Aligned beta release-candidate metadata to `0.9.0+1`, documented the proposed `v0.9.0-beta.1` tag command, added beta acceptance criteria and release blockers/follow-ups, refreshed Slice 41 docs, and kept the RC limited to docs/metadata without publishing, signing, cloud, Firebase runtime, ads, sync, backend activation, or paid services. | `git status --short`; `git rev-parse --short HEAD`; `git log --oneline -8`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `git diff --check`; `git diff --exit-code -- lib test`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/integration`; requested release/metadata/stale-name/TODO/secrets/cloud guardrail `rg` checks. `scripts/check.sh` was blocked by the escalation usage limit after approval review rejected the command; `flutter build apk --debug` was not feasible without escalation because Flutter SDK cache writes failed on the read-only sandbox. | Do not create or push `v0.9.0-beta.1` until the release owner reviews trademark/store-name availability, privacy/data-safety declarations, support/contact links, final screenshots, age rating, and artifact limitations. |
| 2026-06-02 | 42 — Production release checklist | done | 2690f69 | Added an honest production-readiness checklist for future `v1.0.0`, documented blocker status, release-owner decisions, validation gates, platform signing/store requirements, privacy/data-safety/trademark checks, and future-only tag instructions while keeping current beta metadata at `0.9.0+1`. | `git status --short`; `git rev-parse --short HEAD`; `git log --oneline -8`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `git diff --check`; `git diff --exit-code -- lib test`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/integration`; `scripts/check.sh`; `flutter build apk --debug`; requested release/metadata/stale-name/TODO/secrets/cloud guardrail `rg` checks. | Production remains blocked until release-owner approval, trademark/store-name verification, support/contact links, final screenshots, privacy/data-safety and age/content declarations, signed Android App Bundle, iOS archive/TestFlight/App Store path, and final store review are complete. Do not create or push `v1.0.0` before those gates pass. |
| 2026-06-02 | 43 — Official exercise catalog assets and importer | done | 0a7b65e | Added a bundled catalog manifest and parser, bumped the official catalog content version to `2026.06.0`, expanded the curated MVP catalog from 6 to 15 exercises, hardened parser/import validation, and preserved workout-set and workout-group assignment snapshots across version-aware imports. | `git status --short`; `git rev-parse --short HEAD`; `git log --oneline -8`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `git diff --check`; `git diff --exit-code -- lib test`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/features/exercise_catalog`; `flutter test test/src/integration`; `scripts/check.sh`; `flutter build apk --debug`; requested catalog/cloud/domain-purity guardrail `rg` checks. | Keep weighted muscle activation, equipment max-load constraints, remote/static catalog patch channels, custom exercise UI, and recommendation logic for their explicit later slices. |
| 2026-06-02 | 44 — User profile, focus, and equipment domain model | done | feb5af7 | Extended the existing local settings/profile model with optional sex/gender preference, birth year, body weight, height, training goal, recovery sensitivity, coaching strictness, rack equipment, and home-gym max-load/increment constraints, plus additive Drift schema v8 persistence and constraint-aware repository tests. | `git status --short`; `git rev-parse --short HEAD`; `git log --oneline -8`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `git diff --check`; `git diff --exit-code -- lib test`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/features/settings`; `flutter test test/src/features/onboarding`; `flutter test test/src/integration`; `scripts/check.sh`; `flutter build apk --debug`; requested profile/stereotype/cloud/domain-purity guardrail `rg` checks. | Future slices may add compact UI fields and recommendation logic, but must keep sex/gender non-authoritative, preserve local-first defaults, and avoid cloud/profile sync or wearable dependencies unless explicitly scoped. |
| 2026-06-02 | 45 — Muscle activation model | done | fc9ab9f | Added pure-Dart muscle IDs, activation weights, exercise activation profiles, explicit unavailable profile states, confidence-aware muscle-load inputs/estimates, and deterministic per-muscle estimated load aggregation from logged sets without persistence, catalog rewrites, UI, recommendations, recovery logic, wearables, calorie estimates, cloud services, Firebase, sync, or remote catalog fetching. | `git status --short`; `git rev-parse --short HEAD`; `git log --oneline -8`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `git diff --check`; `git diff --exit-code -- lib test || true`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/features/exercise_catalog`; `flutter test test/src/features/training_log`; `flutter test test/src/features/analytics`; `flutter test test/src/integration`; `scripts/check.sh`; `flutter build apk --debug`; requested muscle-activation/no-heatmap/no-cloud/domain-purity guardrail `rg` checks. | Later slices should add catalog-backed activation assets/import mapping and dashboard visualization only in their explicit scopes. |
| 2026-06-02 | 46 — Muscle balance detection | done | this commit | Added pure-Dart focus-aware muscle balance target ranges, confidence-aware balance assessments, insufficient-data handling, explainable push/pull/lower-body and movement-pattern signals, and unknown-activation evidence without UI, persistence, recommendations, recovery logic, wearables, calorie estimates, cloud services, Firebase, sync, or remote catalog fetching. | `git status --short`; `git rev-parse --short HEAD`; `git log --oneline -8`; `flutter pub get`; `flutter gen-l10n`; `dart run build_runner build --delete-conflicting-outputs`; `git diff --check`; `git diff --exit-code -- lib test || true`; `dart format --output=none --set-exit-if-changed .`; `flutter analyze`; `flutter test`; `flutter test test/src/features/analytics`; `flutter test test/src/features/training_log`; `flutter test test/src/features/settings`; `flutter test test/src/integration`; `scripts/check.sh`; `flutter build apk --debug`; requested muscle-balance/no-future-feature/no-cloud/domain-purity guardrail `rg` checks. `build_runner` passed while warning that `--delete-conflicting-outputs` is ignored by the installed version. APK build passed with the existing `in_app_purchase_android` KGP warning. | Future slices should wire catalog-backed movement-pattern/activation inputs, dashboard visualization, recovery/readiness, and recommendations only in their explicit scopes. |
