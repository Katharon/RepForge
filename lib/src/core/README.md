# Core

Shared cross-feature code belongs here only when it is genuinely reused by
multiple features. Keep domain/application code pure Dart and independent from
Flutter, platform APIs, persistence, and remote services.
