# Integration Test Directory

Slice 29 keeps the first workout logging flow harness in
`test/src/integration/` so it runs as a deterministic widget test without a
connected device.

Device-backed Flutter integration tests can be added here in a later slice when
the app has a production workout execution screen.
